# Local config (contiene secrets — SUPABASE_ANON_KEY, REVENUECAT_API_KEY, SENTRY_DSN)
local.properties

# Gradle
.gradle/
build/
**/build/
captures/
.gradletasknamecache

# Nota: el wrapper binary (gradle-wrapper.jar + gradlew + gradlew.bat) SÍ se commitea,
# práctica estándar Android para que cualquier clone permita `./gradlew assembleDebug` directo.

# IDE
.idea/
*.iml
*.iws
*.ipr
.vscode/

# Android Studio
.cxx/
local-build.cache

# Output
*.apk
*.aab
release/

# Keystores (NUNCA commitear)
*.jks
*.keystore
key.properties
keystore.properties

# OS
.DS_Store
Thumbs.db

# Logs
*.log
hs_err_pid*

# Crashlytics / Sentry
google-services.json
sentry.properties
